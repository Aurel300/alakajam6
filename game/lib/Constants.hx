package lib;

enum abstract ItemName(String) from String {
  var Gun;
}

enum abstract RoomName(String) from String {
  var Home;
}

enum abstract CharName(String) from String {
  var Player;
  var Bobbard;
}
